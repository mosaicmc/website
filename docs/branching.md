# Branching Strategy

- Default branch: `main`
- Feature branches: `feat/<topic>`
- Fix branches: `fix/<issue>`
- Release branches: `release/<version>`
- CI required on PRs to `main`.
- Use squash merges with clear messages.

